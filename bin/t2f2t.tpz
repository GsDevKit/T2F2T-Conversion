  set u SystemUser p swordfish
  set solologin on
  login

time
run
	"install the script code into the topaz solo image"
	GsFile sdout nextPutAll: 'Installing t2f2t project'.
	(Rowan
		projectFromUrl: 'file://$binDir/../rowan/specs/T2F2T-Conversion.ston'
		gitUrl: 'file://$binDir/..') load
%

run
	T2F2TConversion executeCommandLine
%
time
