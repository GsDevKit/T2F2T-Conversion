#
  set solologin on
  set u SystemUser p swordfish
  login

	display oops

	run
	| rootDir specFile |
	"install the script code into the topaz solo image"
	rootDir := '$binDir/..' asFileReference pathString.
	specFile := rootDir, '/rowan/specs/tonel.ston' asFileReference pathString.
	[
		(Rowan
			projectFromUrl: 'file://', specFile
			gitUrl: 'file://', rootDir) load ]
		on: Warning
		do: [:ex | 
			"ignore warnings during load"
			ex resume ]
%

run
	T2F2TConversion executeCommandLine
%
